# Obsolete GitHub Pages static site

These files are retained for historical reference only. They are not part of the active Next.js/Vercel production build and must not be treated as current legal content.

The authoritative active legal sources are in `website/content/legal`, with generated clean-route files in `website/public/privacy-policy` and `website/public/terms-of-service`.
